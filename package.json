{
  "name": "stack",
  "version": "1.0.0",
  "description": "inventaire stack visualization in HTML/CSS",
  "scripts": {
    "start": "live-server dist",
    "build": "./scripts/build",
    "watch": "onchange src/* -- npm run build",
    "deploy": "./scripts/deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "maxlath",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.1",
    "onchange": "^7.1.0",
    "sass": "^1.30.0"
  },
  "repository": {
    "type": "git",
    "url": "https://git.inventaire.io/stack"
  }
}
